export const API_URL = `https://forkify-api.jonas.io/api/v2/`;
export const TIMEOUT_SEC = 1000; // In seconds for the timeout function returns if it takes too long to fetch.
export const TESTING = true;
